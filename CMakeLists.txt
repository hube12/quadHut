cmake_minimum_required(VERSION 3.10)
project(QuadHutSeedFinder LANGUAGES C)
set (CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -Wall -g -O3 -static-libgcc -static")
find_package(Threads REQUIRED)
add_subdirectory(src)